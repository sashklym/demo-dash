# Base URL of the backend API. In dev the Vite server runs on 5173 and the API on 3000.
# In production this is baked into the build (see fe/Dockerfile) to point at your deployed BE.
VITE_API_BASE_URL=http://localhost:3000
